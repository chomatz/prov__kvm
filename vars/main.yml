---

kernel:
  arguments:
    amd:
      - amd-iommu=on
      - iommu=pt
      - pci=nommconf
    intel:
      - intel_iommu=on
kvm:
  bridge: kvmbr0
  packages:
    - bridge-utils
    - libvirt
    - qemu-kvm
    - virt-install
    - virt-manager
    - virt-viewer

...
